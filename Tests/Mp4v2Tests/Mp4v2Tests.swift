import XCTest
@testable import Mp4v2

final class Mp4v2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Mp4v2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
